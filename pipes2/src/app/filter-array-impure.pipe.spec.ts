import { FilterArrayImpurePipe } from './filter-array-impure.pipe';

describe('FilterArrayImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArrayImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
