import { YellowBackgroundDirective } from './yellow-background.directive';

describe('YellowBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new YellowBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
